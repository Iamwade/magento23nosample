<?php
/** @var \Akulov\ThreeFormsOfVerbs\Block\Forms $block */
?>
<?php $items = $block->getItems(); ?>
<?php $itemsRand = $block->getRandomItems(); ?>

<div class="forms-table">
    <form action="<?php echo $block->getFormAction() ?>" method="post">
        <table>
            <tbody>
            <tr>
                <td>
                    <?php echo "Number"; ?>
                </td>
                <td>
                    <?php echo "Translate"; ?>
                </td>
                <td>
                    <?php echo "First form"; ?>
                </td>
                <td>
                    <?php echo "Second form"; ?>
                </td>
                <td>
                    <?php echo "Third form"; ?>
                </td>
            </tr>
            <?php foreach ($itemsRand as $itemRand): ?>
                <?php foreach ($items as $item): ?>
                    <?php if ($item->getId() == $itemRand): ?>
                        <tr>
                            <td>
                                <?php echo $item->getId(); ?>
                                <input id="prodId" type="hidden" name="<?php echo $item->getId(); ?>" value="<?php echo $item->getId(); ?>" >
                            </td>
                            <td class="translate">
                                <?php echo $item['translate']; ?>
                            </td>
                            <td>
                                <input name="first<?php echo $item->getId(); ?> " type="text" autocomplete="off">
                            </td>
                            <td>
                                <input name="second<?php echo $item->getId(); ?>" type="text" autocomplete="off">
                            </td>
                            <td>
                                <input name="third<?php echo $item->getId(); ?>" type="text" autocomplete="off">
                            </td>
                        </tr>
                    <?php endif; ?>
                <?php endforeach; ?>
            <?php endforeach; ?>
            </tbody>
        </table>
        <button type="submit">
            Send Three Forms Of Verbs
        </button>
    </form>
</div>
